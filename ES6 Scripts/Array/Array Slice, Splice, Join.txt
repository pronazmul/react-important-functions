   

   const numbers = [2,4,5,6,7,8,9];

   const sliced = numbers.slice(2,5);
   //Sliced From index no 2 to 4....;

   const spliced = numbers.splice(1,3);
   //Spliced from index no 1 to 3 elements...;
   
   const join = numbers.join(' ');
   //Join An array using whiteSpace....;
